# No external runtime dependencies; tests use Python stdlib `unittest`.
# For development you may install tools like `pytest` or linters separately.
python>=3.8
# No external dependencies required (Tkinter is part of the Python standard library)
# If packaging, use the system Python that includes tkinter
