[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "python-port-scanner-app"
version = "0.2"
description = "Lightweight GUI port scanner"
authors = [ { name = "Jamalludeen Karimi" } ]
readme = "README.md"
requires-python = ">=3.8"
